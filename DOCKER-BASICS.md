# Docker από το μηδέν — μαθαίνοντας με πειράματα

**Σκοπός:** να καταλάβεις τι κάνει το Docker αρκετά ώστε να γράψεις **μόνος σου** το Dockerfile του βήματος 3 του [DOCKER-A.md](DOCKER-A.md).

Δεν θα βρεις εδώ έτοιμο Dockerfile. Θα βρεις τα κομμάτια και τα πειράματα που τα κάνουν να κάτσουν στη θέση τους.

**Πώς να το διαβάσεις:** κάθε βήμα έχει *Τρέξε → Παρατήρησε → Γιατί*. Μην προσπεράσεις το «Παρατήρησε». Το νόημα του Docker δεν είναι στη σύνταξη, είναι στο **πότε συμβαίνει τι** — και αυτό φαίνεται μόνο τρέχοντάς το.

Χρόνος: ~45 λεπτά για τα βήματα 1–8.

---

## Βήμα 0 — Ξεκίνα τον daemon

Το `docker` στο terminal είναι απλώς ένας **client**. Στέλνει εντολές σε ένα background process (τον daemon) που κάνει τη δουλειά. Χωρίς αυτόν, το CLI υπάρχει αλλά δεν κάνει τίποτα.

Άνοιξέ το από το **Launchpad** ή με **Cmd+Space** → `Docker`. Στη γραμμή μενού (πάνω δεξιά στην οθόνη) εμφανίζεται εικονίδιο **φάλαινας**:

- **φάλαινα που κινείται** → ακόμα ξεκινάει, περίμενε
- **σταθερή φάλαινα** → έτοιμο

Την πρώτη φορά θέλει 30–60 δευτερόλεπτα. Μετά:

```bash
docker info
```

Αν βγάλει πληροφορίες, είσαι έτοιμος. Αν βγάλει:

```
Cannot connect to the Docker daemon... Is the docker daemon running?
```

τότε ο daemon δεν έχει σηκωθεί ακόμα — περίμενε λίγο και ξαναδοκίμασε.

> **Μην κλείσεις το Docker Desktop** όσο δουλεύεις. Αν το κλείσεις, κάθε εντολή `docker` θα αποτυγχάνει με αυτό το μήνυμα.

---

## Βήμα 1 — Το μοντέλο: image vs container

Αυτή είναι η μία έννοια που αν την καταλάβεις, όλα τα υπόλοιπα είναι λεπτομέρειες.

- **Image** = ένα παγωμένο, read-only στιγμιότυπο ενός filesystem + η εντολή που πρέπει να τρέξει. Δεν εκτελείται. Είναι αρχείο.
- **Container** = μια *εκτέλεση* ενός image. Παίρνει το filesystem του image και του βάζει από πάνω ένα γράψιμο στρώμα.

Σχέση: **image → container** όπως **class → object**, ή **εκτελέσιμο αρχείο → process**. Ένα image, πολλά containers.

### Τρέξε

```bash
docker run --rm -it alpine sh
```

### 🧭 Προσανατολισμός — διάβασέ το πριν πανικοβληθείς

Το prompt σου άλλαξε σε κάτι σαν αυτό:

```
/ #
│ │
│ └── # = είσαι root (μέσα σε container είσαι πάντα root)
└──── / = ο τρέχων φάκελος — εδώ, η ρίζα του filesystem
```

**Δεν κόλλησε τίποτα. Είσαι μέσα στο container.** Το `/ #` είναι απλώς το prompt του, όπως το δικό σου `$` ή `➜`.

Από δω και πέρα, **ό,τι πληκτρολογείς εκτελείται μέσα στο container** — όχι στο μηχάνημά σου. Είναι σαν να άνοιξες terminal σε άλλον υπολογιστή.

| Θέλω να... | Εντολή |
|---|---|
| **βγω έξω** | `exit` (ή `Ctrl+D`) |
| δω πού είμαι | `pwd` |
| δω ποιος είμαι | `whoami` |
| δω τι υπάρχει | `ls /` |

Το `exit` σταματάει το container· το `--rm` το σβήνει· γυρνάς στο κανονικό σου prompt. **Δεν μένει τίποτα πίσω και δεν χαλάει τίποτα.**

#### Τι κατέβασες

Το image **alpine** — μινιμαλιστικό Linux, ~8 MB — από το Docker Hub, το δημόσιο registry. Κατέβηκε αυτόματα επειδή δεν το είχες· την επόμενη φορά θα είναι ακαριαίο.

**Δεν εγκαταστάθηκε τίποτα στο λειτουργικό σου.** Κάθεται σαν αρχείο στον χώρο του Docker. Το βλέπεις με `docker images` και το σβήνεις με `docker rmi alpine`.

### Κοίτα γύρω σου

Τρέξε τα ένα-ένα, μέσα στο container:

```sh
pwd                    # πού είμαι
whoami                 # ποιος είμαι → root
ls /                   # τι υπάρχει → bin, etc, usr... ένα ολόκληρο Linux
cat /etc/os-release    # τι λειτουργικό είναι → Alpine, όχι macOS
ps aux                 # ποια processes τρέχουν → σχεδόν κανένα
```

Το `ps aux` είναι το πιο διαφωτιστικό. Στο μηχάνημά σου τρέχουν εκατοντάδες processes· εδώ βλέπεις **το `sh` σου και τίποτε άλλο**. Αυτό ακριβώς σημαίνει «απομονωμένο».

### Το πείραμα

```sh
echo "γεια" > /test.txt
ls -la /test.txt
exit
```

Τώρα ξανατρέξε **την ίδια ακριβώς εντολή** και ψάξε το αρχείο σου:

```bash
docker run --rm -it alpine sh
```
```sh
ls /test.txt
```

### Παρατήρησε

Το `/test.txt` **δεν υπάρχει**. Χάθηκε.

### Γιατί

Έγραψες στο γράψιμο στρώμα του *πρώτου* container. Το `--rm` το έσβησε στο exit. Το δεύτερο `docker run` έφτιαξε **καινούργιο** container από το ίδιο αμετάβλητο image.

> **Το image δεν αλλάζει ποτέ από τη χρήση.** Ό,τι γράφει ένα container ζει και πεθαίνει μαζί του. Αν θες κάτι μόνιμα μέσα στο image, μπαίνει τη στιγμή του **build**, όχι τη στιγμή του run.

Κράτα αυτή τη φράση. Είναι η ρίζα του βήματος 5.

### Τα flags

| Flag | Τι κάνει |
|---|---|
| `--rm` | σβήσε το container όταν σταματήσει (αλλιώς μαζεύονται) |
| `-it` | interactive + terminal — χρειάζεται για να «μπεις μέσα» |
| `alpine` | το image. Μίνιμαλ Linux, ~8 MB |
| `sh` | override της default εντολής του image |

---

## Βήμα 1b — Τι ακριβώς είναι το «γράψιμο στρώμα»

Είπαμε ότι το container *«παίρνει το filesystem του image και του βάζει από πάνω ένα εγγράψιμο στρώμα»*. Αξίζει να δεις τι σημαίνει αυτό, γιατί εξηγεί τα πάντα από δω και κάτω.

### Το image είναι στοίβα, όχι αρχείο

Ένα layer ανά εντολή του Dockerfile, **όλα read-only**:

```
┌─────────────────────────────┐
│ layer 3:  CMD metadata      │  ← read-only
├─────────────────────────────┤
│ layer 2:  RUN go build →    │  ← read-only
│           /app/server       │
├─────────────────────────────┤
│ layer 1:  COPY . . →        │  ← read-only
│           /app/*            │
├─────────────────────────────┤
│ layer 0:  FROM golang       │  ← read-only
│           /bin /usr /etc... │
└─────────────────────────────┘
```

Το container προσθέτει **ένα** στρώμα — το μόνο εγγράψιμο:

```
┌─────────────────────────────┐
│ container layer    ✏️ RW    │  ← ζει όσο το container
├═════════════════════════════┤
│         image (RO)          │
└─────────────────────────────┘
```

### Δεν το κάνει «εκτελέσιμο»

Συνηθισμένη παρανόηση. Το image δεν του λείπει κάτι για να τρέξει — του λείπει ένα μέρος για να **γράψει**.

Την εκτέλεση την κάνει ο **Linux kernel**: το Docker ξεκινάει κανονικό process, του δίνει αυτό το filesystem ως ρίζα (`/`), και το απομονώνει με namespaces + cgroups. Το εγγράψιμο στρώμα αφορά **αποθήκευση**, όχι εκτέλεση — υπάρχει γιατί κάθε process πρέπει να μπορεί να γράψει *κάπου* χωρίς να μολύνει το image.

### Union filesystem & copy-on-write

Το process δεν βλέπει στοίβα. Βλέπει **ένα ενιαίο filesystem**: το overlayfs συγχωνεύει τα layers και, όταν ζητάς ένα αρχείο, σου δίνει την πρώτη εκδοχή που θα βρει ψάχνοντας από πάνω προς τα κάτω.

| Ενέργεια | Τι συμβαίνει πραγματικά |
|---|---|
| Διαβάζεις αρχείο | Σερβίρεται κατευθείαν από το layer του image. Καμία αντιγραφή. |
| Γράφεις **νέο** αρχείο | Δημιουργείται στο container layer |
| Τροποποιείς **υπάρχον** αρχείο του image | **Αντιγράφεται πρώτα προς τα πάνω**, μετά αλλάζει το αντίγραφο. Το πρωτότυπο μένει άθικτο. |
| Σβήνεις αρχείο του image | Μπαίνει *whiteout* marker που το κρύβει. **Το αρχείο εξακολουθεί να υπάρχει** από κάτω. |

### Τρέξε — «κατάστρεψε» ένα αρχείο συστήματος

```bash
docker run --rm -it alpine sh
```
```sh
cat /etc/os-release          # υπάρχον αρχείο του image
echo "χαλασμένο" > /etc/os-release
cat /etc/os-release          # άλλαξε
exit
```

Ξανατρέξε το ίδιο και κάνε `cat /etc/os-release`.

### Παρατήρησε

**Ανέπαφο.** Πέταξες ένα αρχείο συστήματος και το image δεν το πήρε χαμπάρι.

### Γιατί σε αφορά

- **Το image δεν μπορεί να μολυνθεί από τη χρήση** — κυριολεκτικά δεν γράφεται. Γι' αυτό είναι αναπαραγώγιμο.
- **Δέκα containers μοιράζονται τα ίδια layers** στον δίσκο. Το κόστος ανά container είναι σχεδόν μηδέν — γι' αυτό το `docker run` είναι τόσο φθηνό.
- **Στο δικό σου project:** ο `server` και τα `banners/` ζουν σε read-only layers και διαβάζονται χωρίς αντιγραφή. Αν κάποτε η εφαρμογή γράψει αρχείο στον δίσκο (π.χ. στο Export slice), θα πάει στο εφήμερο στρώμα και θα **εξαφανιστεί** στο restart. Αν πρέπει να επιβιώσει, θέλει *volume* — άλλο κεφάλαιο.

---

## Βήμα 2 — Δες τι υπάρχει στο μηχάνημά σου

```bash
docker images     # τα images (τα «παγωμένα»)
docker ps         # containers που τρέχουν ΤΩΡΑ
docker ps -a      # + όσα σταμάτησαν και δεν σβήστηκαν
```

### Παρατήρησε

Το `alpine` είναι στη λίστα των images — κατέβηκε αυτόματα στο βήμα 1. Το `docker ps` είναι πιθανότατα άδειο (τα containers σου τερμάτισαν και το `--rm` τα καθάρισε).

### Γιατί έχει σημασία

Είναι δύο ξεχωριστές λίστες γιατί είναι δύο ξεχωριστά πράγματα. Όταν αργότερα κάτι δεν δουλεύει, το πρώτο ερώτημα είναι πάντα: *έσκασε το build (image) ή έσκασε το run (container);*

### Πώς διαβάζεται το `docker ps`

```
CONTAINER ID   IMAGE     COMMAND    STATUS         PORTS                    NAMES
a3f9c2e1b8d4   lab       "sh"       Up 2 minutes   0.0.0.0:8080->8080/tcp   nifty_curie
```

- **CONTAINER ID** — το αναγνωριστικό. Όπου ζητείται `<id>`, **αρκούν τα 3-4 πρώτα ψηφία** (π.χ. `a3f9`), αρκεί να είναι μοναδικά
- **NAMES** — τυχαίο όνομα που δίνει το Docker αν δεν όρισες εσύ. Δουλεύει παντού όπου δουλεύει και το ID
- **STATUS** — `Up ...` τρέχει, `Exited (0)` τερμάτισε κανονικά, `Exited (1)` με σφάλμα
- **PORTS** — το mapping. Κενό σημαίνει ότι δεν έδωσες `-p`

### 🛑 Πώς σταματάς ένα container

**Αν τρέχει στο προσκήνιο** (το terminal σου είναι «κολλημένο», βλέπεις logs):

> **Ctrl+C**

Αυτό είναι το 90% των περιπτώσεων. Στο βήμα 8 ο server σου θα κρατήσει το terminal — έτσι τον σταματάς.

**Αν τρέχει στο παρασκήνιο ή σε άλλο tab:**

```bash
docker ps                  # βρες το CONTAINER ID
docker stop a3f9           # σταμάτησέ το με τα πρώτα ψηφία
```

**Αν κόλλησε και δεν σταματάει:** `docker kill a3f9`

**Πανικός — σταμάτα τα πάντα:** `docker stop $(docker ps -q)`
Το `-q` επιστρέφει μόνο τα IDs, και το `$(...)` τα περνάει ως ορίσματα στο `stop`.

---

## Βήμα 3 — Το πρώτο σου Dockerfile

Φτιάξε έναν φάκελο **εκτός** του project για πειράματα:

```bash
mkdir ~/docker-lab && cd ~/docker-lab
```

Μέσα εκεί θα φτιάξεις ένα αρχείο με όνομα **`Dockerfile`** — κεφαλαίο D, **χωρίς κατάληξη**, χωρίς τελεία πουθενά.

### 📝 Πώς φτιάχνεις το αρχείο

Διάλεξε έναν τρόπο. Ο Α είναι ο πιο βολικός αν έχεις ήδη VSCode.

#### Τρόπος Α — VSCode

```bash
code ~/docker-lab
```

Ανοίγει τον φάκελο σε παράθυρο VSCode. Μετά:

1. Στο **Explorer** (αριστερή στήλη), δίπλα στο όνομα του φακέλου, πάτα το εικονίδιο **New File** — σελίδα με `+`
2. Γράψε `Dockerfile` και **Enter**
3. Γράψε το περιεχόμενο
4. **Cmd+S** για αποθήκευση

**Σημάδι ότι το πέτυχες:** κάτω δεξιά στη status bar γράφει *Dockerfile* (όχι *Plain Text*), και το `FROM`/`CMD` χρωματίζονται. Το VSCode αναγνωρίζει το όνομα.

Αν η εντολή `code` δεν βρίσκεται: μέσα στο VSCode πάτα **Cmd+Shift+P** → γράψε `shell command` → διάλεξε *Install 'code' command in PATH*.

#### Τρόπος Β — Terminal, μία εντολή

Φτιάχνει αρχείο **και** περιεχόμενο μαζί:

```bash
cat > Dockerfile << 'EOF'
FROM alpine
CMD ["echo", "γεια από το container"]
EOF
```

Το `cat > Dockerfile` σημαίνει «γράψε στο αρχείο». Το `<< 'EOF'` σημαίνει «πάρε ό,τι ακολουθεί μέχρι να δεις τη λέξη EOF». Γράφεις τις γραμμές, μετά **`EOF` μόνη της σε δική της γραμμή** και Enter.

Τα μονά εισαγωγικά στο `'EOF'` δεν είναι διακοσμητικά — εμποδίζουν το shell να πειράξει χαρακτήρες όπως το `$`.

#### Τρόπος Γ — nano, μέσα στο terminal

```bash
nano Dockerfile
```

Γράφεις κανονικά. Μετά **Ctrl+O** → Enter (αποθήκευση), **Ctrl+X** (έξοδος). Τα `^O` `^X` που βλέπεις κάτω-κάτω σημαίνουν Ctrl+O, Ctrl+X.

### ⚠️ Η παγίδα του macOS — μη χρησιμοποιήσεις TextEdit

Δύο ανεξάρτητα προβλήματα:

**1. Rich text.** Το TextEdit σώζει αόρατη μορφοποίηση. Το Docker θα δει σκουπίδια.

**2. Κρυφή κατάληξη.** Το macOS κρύβει τις καταλήξεις. Σώζεις «Dockerfile» και στην πραγματικότητα γράφεται **`Dockerfile.txt`**. Στο Finder φαίνεται σωστό, και το `docker build` λέει:

```
ERROR: failed to read dockerfile: open Dockerfile: no such file or directory
```

Και ψάχνεις μισή ώρα ένα αρχείο που «είναι εκεί».

### Επαλήθευση πριν προχωρήσεις

```bash
ls -la              # ψάξε γραμμή που τελειώνει σε ' Dockerfile'
cat Dockerfile      # πρέπει να δεις ακριβώς ό,τι έγραψες
file Dockerfile     # πρέπει να λέει 'ASCII text' ή 'Unicode text'
```

Αν το `file` λέει *Rich Text Format*, έπεσες στην παγίδα. Αν το όνομα βγήκε λάθος:

```bash
mv Dockerfile.txt Dockerfile
```

### Το περιεχόμενο για αυτό το βήμα

```dockerfile
FROM alpine
CMD ["echo", "γεια από το container"]
```

### Τρέξε

```bash
docker build -t lab .
docker run --rm lab
```

### Παρατήρησε

Δύο ξεχωριστές φάσεις. Το `build` παρήγαγε image (`docker images` → θα δεις το `lab`). Το `run` έφτιαξε container από αυτό.

### Γιατί

- **`FROM`** — από ποιο υπάρχον image ξεκινάς. Πάντα η πρώτη εντολή. Δεν φτιάχνεις Linux από το μηδέν.
- **`CMD`** — η **default εντολή** που τρέχει όταν ξεκινά container. Δεν εκτελείται στο build.
- **`-t lab`** — δίνει όνομα (tag) στο image. Χωρίς αυτό παίρνει μόνο ένα hash.
- **`.`** στο τέλος — το **build context**: ο φάκελος που στέλνεται στον daemon. Δεν είναι διακοσμητικό, το χρειάζεται το `COPY`.

### Δοκίμασε

```bash
docker run --rm lab echo "κάτι άλλο"
```

Ό,τι γράψεις μετά το όνομα του image **αντικαθιστά** το `CMD`. Γι' αυτό το `CMD` λέγεται *default*.

---

## Βήμα 4 — COPY και WORKDIR

🔴 **Το πιο σημαντικό βήμα του οδηγού.** Εδώ κρύβεται το bug που θα σε φάει στο Docker A.

Βεβαιώσου ότι είσαι στον σωστό φάκελο και φτιάξε ένα αρχείο για δοκιμή:

```bash
cd ~/docker-lab
mkdir data
echo "περιεχόμενο" > data/file.txt
```

> 📌 **«Άλλαξε το Dockerfile» σημαίνει: αντικατέστησε ΟΛΟ το περιεχόμενο.**
> Από δω και κάτω δουλεύουμε πάντα στο **ίδιο** αρχείο `~/docker-lab/Dockerfile`, σβήνοντας ό,τι είχε πριν. Μην προσθέτεις από κάτω — θα καταλήξεις με πολλαπλά `FROM` και μπερδεμένα αποτελέσματα.
> Στο VSCode: **Cmd+A** (επιλογή όλων) → γράψε από πάνω → **Cmd+S**.

Νέο περιεχόμενο:

```dockerfile
FROM alpine
COPY . .
CMD ["ls", "-la"]
```

### Τρέξε

```bash
docker build -t lab .
docker run --rm lab
```

### Παρατήρησε

Τα αρχεία σου προσγειώθηκαν στο `/` (τη ρίζα), ανακατεμένα με `bin`, `etc`, `usr`. Δεν όρισες πού, οπότε πήγαν στο default working directory.

### Τώρα πρόσθεσε WORKDIR

```dockerfile
FROM alpine
WORKDIR /app
COPY . .
CMD ["ls", "-la"]
```

```bash
docker build -t lab . && docker run --rm lab
```

Τώρα βλέπεις **μόνο** τα δικά σου αρχεία. Καθαρά.

### Γιατί — και εδώ είναι το κρίσιμο

Το `WORKDIR` κάνει **δύο** πράγματα, όχι ένα:

1. **Στο build:** ορίζει πού προσγειώνονται τα `COPY` και πού τρέχουν τα `RUN`
2. **Στο run:** ορίζει το **working directory του process**

Το #2 είναι που σε αφορά. Απόδειξέ το:

```dockerfile
FROM alpine
WORKDIR /app
COPY . .
CMD ["cat", "data/file.txt"]
```

```bash
docker build -t lab . && docker run --rm lab
```

Δουλεύει — το `data/file.txt` είναι σχετικό path και επιλύεται ως προς το `/app`.

#### ⚠️ Προσοχή: δεν αρκεί να αλλάξεις το WORKDIR

Ίσως σκεφτείς «βάζω `/somewhere-else` αντί για `/app` και θα σκάσει». **Δεν θα σκάσει** — και ο λόγος είναι το ίδιο το μάθημα.

Αλλάζοντας το ένα `WORKDIR` μετακινείς **και τα δύο** πράγματα ταυτόχρονα: ο προορισμός του `COPY` (η δεύτερη τελεία) είναι σχετικός ως προς το WORKDIR, οπότε **τα αρχεία ακολουθούν**. Μετακίνησες και το σπίτι και τον ένοικο.

Για να δεις την αποτυχία πρέπει να **χωρίσεις** τις δύο λειτουργίες — δεύτερο `WORKDIR` **μετά** το `COPY`:

```dockerfile
FROM alpine
WORKDIR /app
COPY . .
WORKDIR /somewhere-else
CMD ["cat", "data/file.txt"]
```

```bash
docker build -t lab . && docker run --rm lab
```
```
cat: can't open 'data/file.txt': No such file or directory
```

Τα αρχεία προσγειώθηκαν στο `/app` — το WORKDIR που ίσχυε **τη στιγμή του `COPY`**. Το process όμως ξεκινά στο `/somewhere-else`.

Επιβεβαίωσέ το με τα μάτια σου:

```bash
docker run --rm -it lab sh
```
```sh
pwd                    # /somewhere-else — άδειο
ls /app/data           # file.txt — εδώ ήταν όλη την ώρα
exit
```

Ο κώδικας δεν άλλαξε. Το αρχείο **υπάρχει** στο image. Απλώς κανείς δεν το ψάχνει εκεί.

> **Αυτό ακριβώς παθαίνει η εφαρμογή σου.** Το [handlers.go](handlers/handlers.go) κάνει `template.ParseFiles("templates/index.html")` και το [ascii.go](ascii/ascii.go) κάνει `filepath.Join("banners", ...)`. Σχετικά paths, και τα δύο. Αν το `WORKDIR` δεν είναι εκεί που κάθονται τα `templates/` και `banners/`, ο server **σηκώνεται μια χαρά** και κάθε request γυρνάει 500.

### `COPY . .` — τι σημαίνουν οι δύο τελείες

Πρώτη = πηγή, μέσα στο build context (host). Δεύτερη = προορισμός, μέσα στο image, **σχετικά με το WORKDIR**.

---

## Βήμα 5 — RUN vs CMD: η διάκριση που μπερδεύει τους πάντες

```dockerfile
FROM alpine
WORKDIR /app
RUN echo "ΕΓΩ ΤΡΕΧΩ ΣΤΟ BUILD" > /app/proof.txt
CMD ["cat", "/app/proof.txt"]
```

### Τρέξε

```bash
docker build -t lab .          # κοίτα προσεκτικά το output
docker run --rm lab
docker run --rm lab            # και δεύτερη φορά
```

### Παρατήρησε

Το `RUN` εκτελέστηκε **μία φορά**, στο build. Το `proof.txt` υπάρχει τώρα **μέσα στο image**, οπότε κάθε container το βρίσκει έτοιμο. Το `CMD` έτρεξε δύο φορές — μία ανά container.

### Γιατί

| | Πότε | Το αποτέλεσμα |
|---|---|---|
| `RUN` | build | ψήνεται μόνιμα στο image |
| `CMD` | run | ζει όσο το container |

Θυμήσου το βήμα 1: *ό,τι θες μόνιμα, μπαίνει στο build.*

### Η εφαρμογή στο δικό σου project

Η μεταγλώττιση Go είναι κάτι που θέλεις **μία φορά**, όχι σε κάθε εκκίνηση. Άρα το `go build` είναι `RUN`. Το να ξεκινήσει ο server είναι κάτι που θέλεις **σε κάθε container**. Άρα είναι `CMD`.

Αν βάλεις το `go build` σε `CMD`, θα μεταγλωττίζει από την αρχή κάθε φορά που σηκώνεις container. Δουλεύει — και είναι λάθος.

---

## Βήμα 6 — Layers και cache

Κάθε εντολή του Dockerfile φτιάχνει ένα **layer**. Το Docker τα κρατά cached και ξαναχρησιμοποιεί όσα δεν άλλαξαν.

Κράτα το Dockerfile του βήματος 5 ως έχει και κάνε **τρία** builds, παρατηρώντας το output κάθε φορά.

**1ο — χωρίς καμία αλλαγή:**

```bash
docker build -t lab .
```

Θα δεις `CACHED` σχεδόν παντού, και τελειώνει ακαριαία.

**2ο — άλλαξε μόνο την τελευταία γραμμή** (`CMD`) σε:

```dockerfile
CMD ["echo", "άλλαξα μόνο το CMD"]
```

```bash
docker build -t lab .
```

Cached όλα, εκτός από την τελευταία γραμμή.

**3ο — άλλαξε την 3η γραμμή** (`RUN`) σε:

```dockerfile
RUN echo "ΔΙΑΦΟΡΕΤΙΚΟ ΚΕΙΜΕΝΟ" > /app/proof.txt
```

```bash
docker build -t lab .
```

### Παρατήρησε

Στο 3ο build ξαναέτρεξε το `RUN` **και όλα όσα ήταν από κάτω** — παρόλο που το `CMD` δεν το άγγιξες αυτή τη φορά.

### Γιατί

Το cache ακυρώνεται **αλυσιδωτά**: αν ένα layer αλλάξει, όλα τα επόμενα ξαναχτίζονται. Κάθε layer χτίζεται πάνω στο προηγούμενο, δεν μπορεί να μείνει έγκυρο πάνω σε άλλη βάση.

**Πρακτικός κανόνας:** ό,τι αλλάζει σπάνια, πάνω. Ό,τι αλλάζει συχνά, κάτω. Ο πηγαίος κώδικας αλλάζει σε κάθε commit — μπαίνει όσο πιο χαμηλά γίνεται.

---

## Βήμα 7 — Ports

Εδώ χρειάζεσαι **δύο terminal tabs**: στο ένα τρέχει ο server, στο άλλο δοκιμάζεις. Στο Terminal του macOS νέο tab με **Cmd+T**· στο VSCode με το `+` πάνω δεξιά στο panel.

> Γιατί δύο: ένας server δεν τερματίζει — μένει να ακούει. Το πρώτο tab θα «κολλήσει» δείχνοντας logs. **Αυτό είναι το σωστό.** Το σταματάς με **Ctrl+C** όταν τελειώσεις.

Το Dockerfile (χρησιμοποιεί τον ενσωματωμένο web server του busybox, που υπάρχει ήδη στο alpine):

```dockerfile
FROM alpine
RUN mkdir /www && echo "<h1>δουλευει</h1>" > /www/index.html
EXPOSE 9999
CMD ["httpd", "-f", "-p", "9999", "-h", "/www"]
```

Το `-f` σημαίνει *foreground* — χωρίς αυτό ο httpd θα πήγαινε στο παρασκήνιο, το process θα τερμάτιζε και το container θα έκλεινε αμέσως.

### Τρέξε — πρώτα ΧΩΡΙΣ `-p`

**Tab 1:**
```bash
docker build -t lab .
docker run --rm lab
```
Το terminal μένει «κρεμασμένο». Σωστό — ο server ακούει.

**Tab 2:**
```bash
curl localhost:9999
```
```
curl: (7) Failed to connect to localhost port 9999: Connection refused
```

### Τώρα ΜΕ `-p`

Στο **Tab 1** πάτα **Ctrl+C**, μετά:
```bash
docker run --rm -p 9999:9999 lab
```

Στο **Tab 2**:
```bash
curl localhost:9999
```
```html
<h1>δουλευει</h1>
```

Δοκίμασέ το και στον browser: **http://localhost:9999**

Όταν τελειώσεις, **Ctrl+C** στο Tab 1.

### Παρατήρησε

Το `EXPOSE 9999` υπήρχε στο Dockerfile **και στις δύο** περιπτώσεις. Το image ήταν το ίδιο. Μόνο του δεν έκανε απολύτως τίποτα.

### Γιατί

- **`EXPOSE`** = τεκμηρίωση. Δηλώνει πρόθεση σε όποιον διαβάσει το Dockerfile. **Δεν ανοίγει τίποτα.**
- **`-p host:container`** = το πραγματικό mapping.

Το container έχει δικό του δίκτυο. Το `-p 8080:8080` λέει «ό,τι έρθει στο 8080 του μηχανήματός μου, στείλ' το στο 8080 του container».

Γι' αυτό, όταν αργότερα ο server σου λέει *«Server running at localhost:8080»*, αυτό το `localhost` είναι **του container**, όχι δικό σου.

---

## Βήμα 8 — Τώρα γράψε το δικό σου

Έχεις όλα τα κομμάτια. Τέλος τα πειράματα — τώρα δουλεύεις στο πραγματικό project:

```bash
cd ~/Git/Zone01/ascii-art-web-stylize
ls
```

Το `ls` πρέπει να δείξει `main.go`, `go.mod`, `handlers/`, `ascii/`, `templates/`, `banners/`. Αν όχι, είσαι σε λάθος φάκελο — το `Dockerfile` πρέπει να μπει **δίπλα στο `main.go`**, στη ρίζα του project.

Φτιάξε εκεί ένα νέο `Dockerfile` (ίδιοι τρόποι με το βήμα 3) και απάντησε τις ερωτήσεις με τη σειρά — **κάθε απάντηση είναι μία γραμμή**.

**1. Από ποιο image ξεκινάς;**
Χρειάζεσαι τον Go compiler *μέσα* στο image, γιατί εκεί θα γίνει η μεταγλώττιση. Το [go.mod](go.mod) λέει ποια έκδοση. Το επίσημο image λέγεται `golang` και τα tags του είναι εκδόσεις.

**2. Πού μέσα στο image θα ζήσει το project;**
Οποιοδήποτε path, αρκεί να είσαι **συνεπής**. Το `/app` είναι σύμβαση.
→ *Θυμήσου το βήμα 4: αυτό ορίζει και το working directory του process στο runtime.*

**3. Πώς μπαίνουν τα αρχεία μέσα;**
Χρειάζεσαι τον κώδικα, τα `templates/`, τα `banners/`, το `static/`.
→ *Στο slice A αρκεί το χοντρικό. Το selective είναι δουλειά του Docker B.*

**4. Πότε γίνεται η μεταγλώττιση;**
Μία φορά ή σε κάθε εκκίνηση; → *βήμα 5*. Η εντολή είναι `go build -o server .`

**5. Πώς δηλώνεις το port;**
→ *βήμα 7. Και θυμήσου ότι από μόνο του δεν αρκεί — το άλλο μισό είναι στο `docker run`.*

**6. Τι τρέχει όταν ξεκινά το container;**
Το binary που παρήγαγε το βήμα 4.
→ *Γράψ' το ως JSON array: `["./server"]`. Είναι το exec form και είναι το σωστό για CMD.*

### Χτίσε και τρέξε

Πάλι **δύο tabs**, όπως στο βήμα 7.

**Tab 1:**
```bash
docker build -t ascii-art-web .
docker run --rm -p 8080:8080 ascii-art-web
```

Το πρώτο build αργεί λίγο — κατεβάζει το image του `golang`, που είναι αρκετά μεγαλύτερο από το alpine. Όταν δεις `Server running at http://localhost:8080`, το terminal μένει κρεμασμένο. **Σωστό.**

### Τα τρία tests — από το Tab 2

Το «σηκώθηκε» **δεν είναι** επιτυχία. Πρέπει να περάσουν και τα τρία:

```bash
curl -i localhost:8080/
```

```bash
curl -i -X POST localhost:8080/ascii-art -d "text=hello" -d "banner=standard"
```

```bash
curl -i localhost:8080/static/style.css
```

| Test | Τι αποδεικνύει |
|---|---|
| 1ο | βρίσκει το `templates/index.html` |
| 2ο | βρίσκει τα `banners/*.txt` |
| 3ο | δουλεύει ο FileServer με το `static/` |

**Το δεύτερο είναι το σημαντικό** — είναι το μόνο που πιάνει το πρόβλημα του βήματος 4. Αν πάρεις `400` με *"could not open banner file"*, το `WORKDIR` ή το `COPY` σου είναι λάθος.

> 💡 Το `-i` λέει στο curl να δείξει και τα HTTP headers, όχι μόνο το σώμα. Η πρώτη γραμμή της απάντησης (`HTTP/1.1 200 OK`) είναι αυτό που θες να δεις.
>
> Αν κάπου δεις εντολή που τελειώνει σε `\` και συνεχίζει στην επόμενη γραμμή: είναι απλώς σπάσιμο μιας μεγάλης εντολής. Πρέπει να μην υπάρχει **κανένας χαρακτήρας μετά το `\`**, ούτε κενό. Ή γράψ' την όλη σε μία γραμμή, όπως εδώ.

Όταν τελειώσεις, **Ctrl+C** στο Tab 1.

---

## Debugging: όταν κάτι δεν παίζει

**Μπες μέσα στο image και κοίτα με τα μάτια σου:**

```bash
docker run --rm -it ascii-art-web sh
```

Το `sh` παρακάμπτει το `CMD` (βήμα 3). Μέσα:

```sh
pwd          # πού είμαι; ⟵ αυτό είναι το WORKDIR σου
ls -la       # υπάρχουν templates/ banners/ static/ server;
```

Το `pwd` + `ls` απαντάει το 90% των προβλημάτων του Docker A.

**Δες τι έγινε στο build:**
```bash
docker build --no-cache -t ascii-art-web .    # αγνόησε το cache εντελώς
docker history ascii-art-web                  # τα layers με τα μεγέθη τους
```

> ℹ️ Θα δεις αναφορές στο `docker exec` (μπαίνει σε container που **ήδη τρέχει**). Χρήσιμο για debugging, αλλά η *επαλήθευση του filesystem με `docker exec`* είναι παραδοτέο του **Docker B**. Χρησιμοποίησέ το για να λύσεις προβλήματα· μην το γράψεις στο σημείωμα σαν δική σου δουλειά.

---

## Καθάρισμα

Τα πειράματα αφήνουν σκουπίδια:

```bash
docker images                 # δες τι μαζεύτηκε
docker rmi lab                # σβήσε το πειραματικό
rm -rf ~/docker-lab
```

> ⚠️ Το `docker system prune` σβήνει **όλα** τα αχρησιμοποίητα images/containers/networks — και άλλων project. Το garbage collection είναι παραδοτέο του **Docker C**· άφησέ το.

---

## Το σύνολο σε 6 γραμμές

1. **Image** = παγωμένο filesystem. **Container** = εκτέλεσή του. Το image δεν αλλάζει ποτέ από τη χρήση.
2. **`RUN`** τρέχει στο build και ψήνεται. **`CMD`** τρέχει σε κάθε container.
3. **`WORKDIR`** ορίζει πού πέφτουν τα `COPY` **και** το working directory του process — άρα πού επιλύονται τα σχετικά paths.
4. Κάθε εντολή = **layer**. Αλλάζει ένα, ξαναχτίζονται όλα από κάτω.
5. **`EXPOSE`** είναι σχόλιο. Το `-p` κάνει τη δουλειά.
6. Το «σηκώθηκε» δεν είναι test. **Κάνε πραγματικό request.**

Με αυτά, το [DOCKER-A.md](DOCKER-A.md) διαβάζεται σαν εφαρμογή γνωστών εννοιών αντί για συνταγή.
